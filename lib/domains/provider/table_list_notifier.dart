// Use Future Provider
// When open app -> watch the tableList from API;
// When user tap on Check in button -> read and fire post request to API (update Table Request);
// When user tap on Check out button -> read and fire post request to API (update Table Request);

// _updateTableStatus(Table table,status) async {
//  after update -> also fetch the tableList from API
// }
// checkInTable(Table table) async {}
// getCheckOutItems(Table t) async {
//   disploy checkoutItem before checkOutTable
// }
// checkOutTable(Table table) async {
// 